/**
 * Route — /media
 *
 * Media Library page. Thin route shell — business logic is in the feature.
 */

import { MediaLibraryPage } from '@/features/media-library';

export default function MediaPage() {
  return <MediaLibraryPage />;
}

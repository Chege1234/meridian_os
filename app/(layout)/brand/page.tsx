/**
 * Route — /brand
 *
 * Brand Center page. Thin route shell — business logic is in the feature.
 */

import { BrandCenterPage } from '@/features/brand-center';

export default function BrandPage() {
  return <BrandCenterPage />;
}
